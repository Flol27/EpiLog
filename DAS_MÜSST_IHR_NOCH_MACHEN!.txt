git clone https://github.com/Flol27/EpiLog.git
cd epilog

npm install

## Eure Environment-Variablen definieren (durch .env File) und Datenbank erstellen.
cp example.env .env
npx prisma generate
npx prisma migrate dev --name init

npm run dev

###################
Das npm install machen
damit installiert man noch node_modules lokal bei sich, 
weil das ja nicht im Repo ist.

