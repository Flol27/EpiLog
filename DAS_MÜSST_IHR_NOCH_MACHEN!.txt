git clone https://github.com/EUER-REPO
cd epilog
npm install

npx prisma generate
npx prisma migrate dev --name init
DATABASE_URL='file:./epilog.sqlite' --> in .env file

npm run dev

Das npm install machen
damit installiert man noch node_modules lokal bei sich, 
weil das ja nicht im Repo ist.
